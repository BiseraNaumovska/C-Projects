/* Program p06_13.c */
/* vnesuvanje na podatoci so dolzhina na pole */

#include <stdio.h>

int main()
{
    int x;
    int y;

    printf( "Vnesi shestcifren broj: " );
    scanf( "%2d%d", &x, &y );

    printf( "Vnesenite celi broevi se %d i %d\n", x, y );

    return 0;
}
